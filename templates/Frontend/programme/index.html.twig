{% extends 'base.html.twig' %}

{% block title %}Hello ProgrammeController!
{% endblock %}

{% block body %}
	<section class="content">
		{% for programme in programmes %}
			<a href="{{path('user.programmes.list',{slug:programme.categorie.slug})}}">
				<div class="proList">
					<p>{{programme.categorie.name}}</p>
				</div>
			</a>
		{% endfor %}
	</section>
{% endblock %}
