{{form_start(form,{'attr': {'class': "card p-3 w-50 mx-auto mt-3"}})}}
{{form_widget(form)}}
<button type="submit" class='button btn-principal'>{{labelButtons|default('save')}}</button>
{{form_end(form)}}